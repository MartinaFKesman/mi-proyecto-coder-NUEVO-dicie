from django.contrib import admin 
from django.urls import path 

from AppPolyvintage.views import curso, lista_curso 

urlpatterns = [
    path('admin/', admin.site.urls),
    path(<compra>/),
    path('lista_curso/',),
    path("Informacion")
    
    
]
[
    
    
]
